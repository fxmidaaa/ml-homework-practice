[project]
name = "workshop"
version = "0.1.0"
description = "churn prediction service"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.137.2",
    "scikit-learn>=1.9.0",
    "uvicorn>=0.49.0",
]

[dependency-groups]
dev = [
    "requests>=2.34.2",
]
